apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  name: netcore-image-stream
spec:
  params:
    - name: url
      value: image-registry.openshift-image-registry.svc:5000/demo-refdoc/demo-refdoc-webapp
  type: image
 